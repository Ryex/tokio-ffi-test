type ::std::option::Option< <%- self.ty %> > {
    #layout(size = <%- self.size %>, align = 8);
    wellknown_traits(Debug, Copy);
    
    constructor None;
    constructor Some(<%- self.ty %>);

    fn unwrap(self) -> <%- self.ty %>;
    fn expect(self, &str) -> <%- self.ty %>;
}
